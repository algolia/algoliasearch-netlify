{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "baseUrl": ".",
    "outDir": "./dist", /* Redirect output structure to the directory. */
  },
  "include": [
    "src"
  ]
}
